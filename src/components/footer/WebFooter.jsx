import React, {useEffect, useRef, useState} from "react";
import {Flex, Typography} from "antd";

const {Text} = Typography;
const WebFooter = ({isFooterHidden}) => {
    return (
        <>
            Footer
        </>
    )
}

export default WebFooter;
