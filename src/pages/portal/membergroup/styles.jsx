import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ css, token }) => ({
    drawerClickableRow: css`
       position: relative;
  `,
    drawerButton: css`

    `
}));